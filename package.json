{
  "name": "contactpit-website",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build:i18n": "node scripts/build-i18n-from-ios.mjs",
    "dev": "vite",
    "build": "npm run build:i18n && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.19"
  },
  "dependencies": {
    "@phosphor-icons/web": "^2.1.2"
  }
}
